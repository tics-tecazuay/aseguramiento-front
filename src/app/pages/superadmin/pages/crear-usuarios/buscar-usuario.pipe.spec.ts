import { BuscarUsuarioPipe } from '../../../../shared/buscar-usuario.pipe';

describe('BuscarUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
